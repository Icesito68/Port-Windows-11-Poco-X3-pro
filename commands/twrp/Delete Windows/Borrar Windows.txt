#####push parted script to sdcard######
adb push parted /sdcard/

####iniciar adb shell#####
adb shell

#####mover parted a directorio sbin###
cp /sdcard/parted /sbin/ && chmod 755 /sbin/parted

#desmontar data y sdcard
umount /data && umount /sdcard

#Iniciar parted
parted /dev/block/sda

#Eliminar particiones rm {número partición} #
rm 30

rm 29

rm 28

#####Restaurar particiones#########

#Modelos de 128Gb
mkpart userdata ext4 11.8GB 127GB

#Modelos de 256Gb
mkpart userdata ext4 11.8GB 255GB

#Quit
quit

#reiniciar a recovery twrp

#Formatear desde wipe twrp escribiendo yes

#Comprueba que inicia Android